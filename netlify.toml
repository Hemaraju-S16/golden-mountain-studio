[build]
  functions = "netlify/functions"
  publish = "."

[dev]
  functions = "netlify/functions"

# Redirect all routes to index.html for single-page app
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
